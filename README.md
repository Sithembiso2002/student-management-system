# Student Management System (React + Vite + Express + MySQL)

## What you get
- **frontend/** → Vite + React app (Tailwind CSS) – modern, responsive UI  
- **backend/** → Express server with REST API routes for managing students  
- **db.sql** → MySQL schema to create the `students` table  
- **.env.example** → Sample environment variables for backend setup  

---

## 🚀 How to run locally

### 1. Backend
```bash
cd backend
npm install
